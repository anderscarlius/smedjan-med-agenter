# Portal

- `demo/` — **körbar mock-demo** (statisk webbapp) för intern visning/deploy. Starta därifrån.
- `sitemap.md` + `views/` — tidig sidkarta (wireframe-anteckningar från etapp 0).

För VGR-intern deploy: använd `demo/` som document root. Se `demo/README.md`.
