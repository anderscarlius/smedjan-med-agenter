# Mallar

- `*.md` — mallar för agenter och Git (sanningen i processen).
- `word/` — samma mallar som Word (`.docx`) för människor; konverteras till HTML/Markdown med pandoc. Se `word/README.md`.
- Ifyllda exempel: `../exempel/`.
